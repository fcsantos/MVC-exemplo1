@model Projeto.MVC.Models.EditoraViewModelConsulta

@{
    ViewBag.Title = "Consulta";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<br />
<h2>Lista de Editoras</h2>
<br />
<div class="alert-danger">
    @Html.DisplayFor(model => model.MensagemErro)
</div>
<hr />
@if (Model.Editoras.Count != 0)
{
    <table class="table table-bordered">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdEditora)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayName("Opções")
            </th>
        </tr>

        @foreach (var item in Model.Editoras)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.IdEditora)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Nome)
                </td>
                <td>
                    @Html.ActionLink("Atualizar", "Atualiza", new { id = item.IdEditora }, new { @class = "btn btn-primary") |
                    @Html.ActionLink("Deletar", "Deleta", new { id = item.IdEditora }, new { onclick = "return confirm('Tem certeza que deseja deletar?');", @class = "btn btn-danger" })
                </td>
            </tr>
        }
    </table>
}
else
{
    <div class="alert alert-warning">
        Não tem registros.
    </div>
}
